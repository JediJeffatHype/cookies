module.exports = function cookies(input) { //open code
    if (typeof input == "number") console.log('you put in a number') //detects if its an integer/number
    if (typeof input == "boolean") console.log('true/false statement') //detects if it is a boolean
    if (typeof input == "string") console.log('string!') //detects if it is a string
} //end code
